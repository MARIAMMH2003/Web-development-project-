<div class='topnav'>
  <a href='/'>Home</a>
  <a href='/slide'>Gallery</a>
  <a href='/about'>About</a>

  <% if (!user) { %>
      <a href='/signup'>Sign Up</a>
      <a href='/login'>Login</a>
  <% } else { %>
      <% if (user.Type === "admin") { %>
          <a href='/admin/viewAll'>Admin</a>
      <% } %>
      <a href='/user/profile'>Profile</a>
      <a href='/user/logout'>Logout</a>

      <div class="user">
          Welcome <%= user.name %>
      </div>
  <% } %>
</div>